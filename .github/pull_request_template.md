## What this changes

<!-- One or two sentences. Claims of user-visible behavior belong here, and each one needs a test named below. -->

## Checklist

- [ ] Every user-visible claim in this description has a test that RENDERS or exercises it through the real entry point (page, component, route handler, CLI). Name them: <!-- path::test name -->
- [ ] No test added here asserts on source TEXT with a grep/regex where a render or a call would do.
- [ ] Every new module has at least one production caller — or the description says why it ships uncalled.
- [ ] Migrations, env vars, and data changes are listed here with how each was verified.

<!-- See "A claimed user-visible behavior needs a test that renders it" in CLAUDE.md. -->
